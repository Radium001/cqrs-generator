using Application.Common.Interfaces;
{{ usings_block }}

namespace {{ namespace }}
{
    public class {{ command_type }} : {{ command_interface }}
    {
{{ if has_properties }}        public {{ command_type }}({{ constructor_parameters }})
        {
{{ constructor_assignments }}        }

{{ properties_block }}
{{ end }}    }
}
