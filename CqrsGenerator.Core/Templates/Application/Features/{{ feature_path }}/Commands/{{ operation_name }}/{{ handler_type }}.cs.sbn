using MediatR;
using System;
using System.Threading;
using System.Threading.Tasks;
{{ usings_block }}
namespace {{ namespace }}
{
    public class {{ handler_type }} : IRequestHandler<{{ command_type }}>
    {
{{ if has_dependencies }}{{ dependencies_fields }}

        public {{ handler_type }}({{ dependencies_params }})
        {
{{ dependencies_assignments }}        }

{{ end }}        public {{ if is_async }}async {{ end }}Task Handle({{ command_type }} request, CancellationToken cancellationToken)
        {
            {{ handler_body }}
        }
    }
}
