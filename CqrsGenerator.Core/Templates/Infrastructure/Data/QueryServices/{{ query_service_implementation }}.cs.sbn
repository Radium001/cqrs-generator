using Application.Common.Interfaces;
using {{ interface_namespace }};
using Infrastructure.Common.Abstractions;
using System;
{{ if needs_collections }}using System.Collections.Generic;
{{ end }}{{ if usings_block != "" }}{{ usings_block }}
{{ end }}{{ if dto_namespace != "" }}using {{ dto_namespace }};
{{ end }}using System.Threading;
using System.Threading.Tasks;

namespace {{ namespace }}
{
    public class {{ query_service_implementation }} : BaseQueryService, {{ query_service_interface }}
    {
        public {{ query_service_implementation }}(IDbConnectionFactory dbConnectionFactory)
            : base(dbConnectionFactory)
        {
        }
{{ if has_method }}
        public {{ if method_is_async }}async {{ end }}{{ method_return_type }} {{ method_name }}({{ method_parameters }})
        {
            {{ implementation_body }}
        }
{{ end }}    }
}
